# This file contains the credentials for the voice & chat platforms
# which your bot is using.
# https://rasa.com/docs/rasa/messaging-and-voice-channels

rest:
  # enable the rest channel for communication with your frontend
  # No need to add anything here - this channel doesn't require credentials
  # If you're using the default REST channel, this will work out of the box.

# Uncomment and configure the following if you need integrations with other channels:

#facebook:
#  verify: "<verify>"
#  secret: "<your secret>"
#  page-access-token: "<your page access token>"

#slack:
#  slack_token: "<your slack token>"
#  slack_channel: "<the slack channel>"
#  slack_signing_secret: "<your slack signing secret>"

#socketio:
#  user_message_evt: <event name for user message>
#  bot_message_evt: <event name for bot messages>
#  session_persistence: <true/false>

#mattermost:
#  url: "https://<mattermost instance>/api/v4"
#  token: "<bot token>"
#  webhook_url: "<callback URL>"

# For Rasa Enterprise users: Configuration to access Rasa Enterprise channels (e.g., for testers).
rasa:
  url: "http://localhost:5002/api" # Ensure this URL corresponds to your Rasa Enterprise instance
